package su.afk.yummy.tv.feature.player.mobile.model

internal enum class MobilePlayerTrackSettingsTab {
    Dubbing,
    Player,

    /** Alloha's own dubbing and subtitle lists, shown as two sections on one tab. */
    Alloha,
}
